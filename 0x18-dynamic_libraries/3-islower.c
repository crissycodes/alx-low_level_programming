#include "main.h"

/**
 * _islower - checks if a character is lowercase
 * @ch: is the character to be checked
 * Return: 1 if ch is lowercase, or 0 if otherwise
 */

int _islower(int ch)
{
	return (ch >= 'a' && ch <= 'z');
}
