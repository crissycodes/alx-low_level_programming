#include "main.h"

/**
 * _isupper - checks if a character is upper
 * @c: character to be checked
 * Return: 1 if c is upper and 0 if not
 */

int _isupper(int c)
{
	return (c >= 'A' && c <= 'Z');
}
